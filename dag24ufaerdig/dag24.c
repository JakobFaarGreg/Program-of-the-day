#include <stdio.h>
#include <stdlib.h>
#include "funktion.h"

int main() {
    int i = 0;
    array = generate_pointer();
    printf("%s, %d, %d", array[0].name, array[0].heigth, array[0].age);
    return EXIT_SUCCESS;
}
